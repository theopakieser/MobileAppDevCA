<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Normal app theme -->
    <style name="Theme.BookApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/colorBackground</item>
    </style>

    <!-- Splash theme  -->
    <style name="Theme.BookApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_book</item>
        <item name="postSplashScreenTheme">@style/Theme.BookApp</item>
    </style>


</resources>
